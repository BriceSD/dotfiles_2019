# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
set $fonttype "pango:Terminus 10"
font $fonttype
#font xft:Source\ Code\ Pro 10

# workspaces
set $WS1 1
set $WS2 2
set $WS3 3
set $WS4 4: www
set $WS5 5
set $WS6 6
set $WS7 7
set $WS8 8: Music
set $WS9 9: Mail
set $WS10 10: DL

# assigns
assign [class="Clementine"] → $WS8
assign [class="JDownloader"] → $WS10

# floating by default
## Firefox
for_window [title="Library"] floating enable
for_window [title="Firefox Preferences"] floating enable

# Bind applications
## launch firefox
bindsym $mod+x exec --no-startup-id firefox

## launch intellij idea
bindsym $mod+y exec --no-startup-id sh $HOME/IntelliJIdea/bin/idea.sh
#
## launch pycharm
bindsym $mod+agrave exec --no-startup-id sh $HOME/Pycharm/bin/pycharm.sh

## launch urxvt (terminal)
bindsym $mod+Return exec urxvt

# Navigation
## Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

## enable workspace back and forth
workspace_auto_back_and_forth yes

## use d to switch back and forth
bindsym $mod+d workspace back_and_forth

##
bindsym $mod+Shift+d move container to workspace back_and_forth; workspace back_and_forth

##
bindsym $mod+b exec --no-startup-id quickswitch.py

##
bindsym $mod+Shift+b exec --no-startup-id quickswitch.py -m

## kill focused window
bindsym $mod+Shift+j kill

## start dmenu (a program launcher)
bindsym $mod+e exec --no-startup-id i3-dmenu-desktop

## change focus
bindsym $mod+c focus left
bindsym $mod+t focus down
bindsym $mod+s focus up
bindsym $mod+r focus right

## move focused window
bindsym $mod+Shift+c move left
bindsym $mod+Shift+t move down
bindsym $mod+Shift+s move up
bindsym $mod+Shift+r move right

# change systeme volume
bindcode 123 exec --no-startup-id amixer set Master 5%+ unmute -q
bindcode 122 exec --no-startup-id amixer set Master 5%- unmute -q
bindcode 121 exec --no-startup-id amixer set Master toggle -q

# clementine Remote control
#bindsym XF86AudioPlay exec clementine_CTRL.py pause
#bindsym Shift+XF86AudioPlay exec clementine_CTRL.py stop
#bindsym Control+XF86AudioPlay exec clementine_CTRL.py osd
#bindsym Control+XF86AudioRaiseVolume exec clementine_CTRL.py next
#bindsym Control+XF86AudioLowerVolume exec clementine_CTRL.py previous

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+i fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+u layout stacking
bindsym $mod+eacute layout tabbed
bindsym $mod+o layout default

# toggle tiling / floating
bindsym $mod+Shift+BackSpace floating toggle

# change focus between tiling / floating windows
bindsym $mod+BackSpace focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+à focus child

# jump to last urgent window
bindsym $mod+g [urgent=latest] focus

# switch to workspace
bindsym $mod+multiply workspace   $WS1
bindsym $mod+w workspace          $WS2
bindsym $mod+quotedbl workspace   $WS3
bindsym $mod+apostrophe workspace $WS4
bindsym $mod+grave workspace      $WS5
bindsym $mod+underscore workspace $WS6
bindsym $mod+ccedilla workspace   $WS7
bindsym $mod+colon workspace      $WS8
bindsym $mod+semicolon workspace  $WS9
bindsym $mod+VoidSymbol workspace $WS10

# move focused container to workspace
bindsym $mod+Shift+multiply move container to workspace    $WS1
bindsym $mod+Shift+w move container to workspace           $WS2
bindsym $mod+Shift+quotedbl move container to workspace    $WS3
bindsym $mod+Shift+apostrophe move container to workspace  $WS4
bindsym $mod+Shift+grave move container to workspace       $WS5
bindsym $mod+Shift+underscore move container to workspace  $WS6
bindsym $mod+Shift+ccedilla move container to workspace    $WS7
bindsym $mod+Shift+colon move container to workspace       $WS8
bindsym $mod+Shift+semicolon move container to workspace   $WS9
bindsym $mod+Shift+VoidSymbol move container to workspace  $WS10

# reload the configuration file
bindsym $mod+Shift+X reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+p restart
# exit i3 (logs # exit i3 (logs you out of your X session)
bindsym $mod+Shift+o exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym c resize shrink width 10 px or 10 ppt
        bindsym t resize grow height 10 px or 10 ppt
        bindsym s resize shrink height 10 px or 10 ppt
        bindsym r resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+p mode "resize"

# This is awesome, see : http://www.slackword.net/?p=657
mode "focused" {

                # hardcoded focus keybindings
                bindsym i [class="(?i)firefox"] focus
                bindsym e [class="(?i)thunderbird"] focus
                bindsym u [class="(?i)zathura"] focus

                # keybindings for marking and jumping to clients
                bindsym a exec i3-input -F 'mark %s' -P 'Mark name: '
                bindsym comma exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

                # Assign marks to keys 1-5
                bindsym Shift+multiply mark mark1
                bindsym Shift+w mark mark2
                bindsym Shift+quotedbl mark mark3
                bindsym Shift+apostrophe mark mark4
                bindsym Shift+grave mark mark5

                # Jump to clients marked 1-5
                bindsym multiply [con_mark="mark1"] focus
                bindsym w [con_mark="mark2"] focus
                bindsym quotedbl [con_mark="mark3"] focus
                bindsym apostrophe [con_mark="mark4"] focus
                bindsym grave [con_mark="mark5"] focus

                # Exit to the default mode
                bindsym Return mode "default"
                bindsym Escape mode "default"
}

bindsym $mod+n mode "focused"



# Start i3bar to display a workspace bar (plus the system information i3status finds out, if available)
bar {
  position top
 	status_command $HOME/.i3/conky/conky-i3bar
}


# Exec on startup
## take care of automounting
exec --no-startup-id "devmon --no-gui"

## Background
exec --no-startup-id feh --bg-scale $HOME/.i3/wallpaper/Stained-Glass-Vector-Lines.jpg

## hide mouse cursor when not used
exec_always --no-startup-id unclutter -idle 1 -jitter 2 -root &

## Keyboard layout
exec_always --no-startup-id xkbcomp -I$HOME/.xkb $HOME/.xkb/jeopPETM.xkb $DISPLAY
exec_always --no-startup-id sh $HOME/.xkb/jeopPETMxCape.sh

## Music
exec --no-startup-id i3-msg 'workspace $WS8; exec clementine'

## JDownloader
exec --no-startup-id i3-msg 'workspace $WS10; exec /bin/JDownloader'

## Terminal
exec --no-startup-id i3-msg 'workspace $WS3; exec urxvt'
